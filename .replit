run = "uvicorn main:app --host 0.0.0.0 --port 8080"

[nix]
channel = "stable-24_05"
packages = ["python3", "pip"]

[deployment]
run = ["sh", "-c", "uvicorn main:app --host 0.0.0.0 --port 8080"]
build = ["sh", "-c", "pip3 install -r requirements.txt"]
